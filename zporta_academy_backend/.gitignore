# In zporta_academy_backend/.gitignore

# Python
venv/
env/
__pycache__/
*.py[cod]
*.sqlite3
db.sqlite3

# Django media/static (if generated by Django's collectstatic and not source files)
media/
staticfiles/

# Logs
*.log

# Secrets / Configs within backend
.env # For a .env file directly inside zporta_academy_backend/
zporta/settings/local.py
zporta/settings/dev.py   # If it contains secrets
zporta/firebase_credentials.json # This is correct for your backend credentials

# Build files (if your backend has specific build outputs, e.g. compiled binaries)
# If your backend also generates a 'build/' or 'dist/' folder, you can list them here:
# build/
# dist/
*.egg-info/

# OS / Editor files (can be here or in a root .gitignore)
.DS_Store
Thumbs.db
.vscode/
.idea/